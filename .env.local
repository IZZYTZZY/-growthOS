# =============================================================
#  .env.local — Copy this file and fill in your values.
#  NEVER commit .env.local to git.
# =============================================================

# ── Supabase ──────────────────────────────────────────────────
# Found at: https://app.supabase.com → Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xmzcjdqnznirtxvyecmm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhtemNqZHFuem5pcnR4dnllY21tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk3MjY3OTcsImV4cCI6MjA5NTMwMjc5N30.u7HTVJ7jb33GaZPrGl_NoM1GRC9PTtWCrdTFZ54w9fE

# Service role key — only used server-side (analytics ingestion, admin ops)
# NEVER expose this to the browser
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhtemNqZHFuem5pcnR4dnllY21tIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3OTcyNjc5NywiZXhwIjoyMDk1MzAyNzk3fQ.rFVWGrzsUc4F9QXMB_PIHB-6x43jbLl6qXbzyF0-JT0

# ── OpenAI ────────────────────────────────────────────────────
# Leave blank to use the built-in template fallback (no cost, no API needed)
OPENAI_API_KEY=sk-...

# ── App ───────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Token encryption ─────────────────────────────────────────
# Used by pgcrypto to encrypt Instagram access tokens at rest.
# Generate with: openssl rand -base64 32
TOKEN_ENCRYPTION_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET

# ── Vercel CRON protection ────────────────────────────────────
# Sent as x-cron-secret header by Vercel CRON jobs
# Generate with: openssl rand -hex 32
CRON_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET
NEXT_PUBLIC_META_APP_ID=1470219664787902